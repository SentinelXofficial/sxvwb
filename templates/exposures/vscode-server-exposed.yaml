id: vscode-code-server-exposed
brief:
  title: VS Code Server / code-server Exposed
  by: sxsc
  level: high
  about: Remote VS Code instance accessible — full server terminal access possible.
  label: [exposure, vscode, ide, rce, remote-access]
  score: "8.8 HIGH"
moves:
  - verb: GET
    to:
      - "{{BaseURL}}/login?folder=/"
      - "{{BaseURL}}/?folder=/"
    signs:
      - on: word
        has: ["code-server", "VS Code", "Visual Studio Code", "vscode"]
        need: any