id: ip-camera-panels
brief:
  title: IP Camera / NVR Panels
  by: sxsc
  level: high
  about: Discovers IP camera and NVR panels — unauthorized surveillance access possible.
  label: [iot, camera, nvr, surveillance, admin]
  score: "7.5 HIGH"
moves:
  - verb: GET
    to:
      - "{{BaseURL}}/"
      - "{{BaseURL}}/doc/page/login.asp"
      - "{{BaseURL}}/cgi-bin/
      - "{{BaseURL}}/web/login.html"
    signs:
      - on: word
        has: ["Hikvision", "Dahua", "Axis", "IP Camera", "NVR", "Live View"]
        need: any