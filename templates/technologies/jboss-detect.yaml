id: jboss-detection
brief:
  title: JBoss / WildFly Detection
  by: sxsc
  level: info
  about: Detects JBoss Application Server or WildFly.
  label: [tech, jboss, wildfly, java]
moves:
  - verb: GET
    to:
      - "{{BaseURL}}/"
      - "{{BaseURL}}/web-console/"
      - "{{BaseURL}}/jmx-console/"
    signs:
      - on: word
        has: ["JBoss", "WildFly", "jboss"]
        need: any